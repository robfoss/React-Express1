// Write a function that returns the birthdate of employee[0]

// Write a function that returns the jobArea of employee[0]

// Write a function that expects to receive a key and returns the corresponding 
// value from employee[0].
// For example:
console.log(getValue('suffix));
// "II"